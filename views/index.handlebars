<h1>Burgerssssss!</h1>

<h2>What kind of Burgers do your like</h2>



<ul>
  {{#each Burgers}}
    {{#unless devoured}}
      {{> Burgers/Burgers-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers are good!</h2>

<ul>
  {{#each Burgers}}
    {{#if devoured}}
      {{> Burgers/Burgers-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burgers</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burgers_name">
  </div>

  {{!-- <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Yummmm!<br>
    <input type="radio" name="devoured" value="0"> Ewwww
  </div> --}}

  <button type="submit">Add burger</button>
</form>
